import { Routes } from '@angular/router';
import { DailyPhotoComponent } from './pages/daily-photo.component';

export const routes: Routes = [
  {
    path: '',
    component: DailyPhotoComponent,
  },
];
